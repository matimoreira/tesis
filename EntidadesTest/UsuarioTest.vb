Imports Entidades
Module UsuarioTest
    Sub main()
        'Dim arrUsuarios As New List(Of Usuario)
        'Dim usuario As New Usuario
        'arrUsuarios.Add(usuario)
        'Dim usuario2 As New Usuario(1, "nick1", "pass1")
        'arrUsuarios.Add(usuario2)

        'For Each user In arrUsuarios
        '    Console.WriteLine(user)
        'Next

        Console.ReadKey()
    End Sub
End Module
